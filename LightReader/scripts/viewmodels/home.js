define(function (require) {
    var app = require('durandal/app'),
        ko = require('knockout');
        jquery = require('jquery');
        chocolate = require('chocolate');

    return {
        name: ko.observable(),

    };
});